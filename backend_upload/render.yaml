services:
  - type: web
    name: storytimecaptions-backend
    env: python
    rootDir: backend_upload
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
